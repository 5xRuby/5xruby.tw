<h1><%%= <%= class_name %>.model_name.human %> <%%= link_to t('crud.new'), new_<%= singular_table_name %>_path, class: 'btn btn-lg btn-success' %></h1>

<%%= paginate @<%= plural_table_name %> %>

<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <%%= th_for <%= class_name %>, :id, <%= attributes.reject(&:password_digest?).map{|a| ":#{a.name}"}.join(', ') %> %>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%% @<%= plural_table_name %>.each do |<%= singular_table_name %>| %>
      <tr>
        <%%= td_for <%= singular_table_name %>, :id, <%= attributes.reject(&:password_digest?).map{|a| ":#{a.name}"}.join(', ') %> %>
        <%%= td_action_for <%= singular_table_name %> %>
      </tr>
    <%% end %>
  </tbody>
</table>

<%%= paginate @<%= plural_table_name %> %>