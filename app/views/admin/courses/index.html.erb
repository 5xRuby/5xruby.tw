<h1><%= Admin::Course.model_name.human %> <%= link_to t('crud.new'), new_admin_course_path, class: 'btn btn-lg btn-success' %></h1>

<%= paginate @admin_courses %>

<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <%= th_for Admin::Course, :id, :image, :title, :summary, :description, :what_will_learn %>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_courses.each do |admin_course| %>
      <tr>
        <%= td_for admin_course, :id, :image, :title, :summary, :description, :what_will_learn %>
        <%= td_action_for admin_course %>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @admin_courses %>