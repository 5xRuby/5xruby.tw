<table class="table table-striped table-bordered">
  <thead>
    <th>屬性</th>
    <th>值</th>
  </thead>
  <tbody>
    <%= tr_for @admin_speaker, :id, :avatar, :name, :title, :role_text, :summary, :is_online %>
  </tbody>
</table>
<div class="btn-group">
  <%= link_to t('crud.edit'), edit_admin_speaker_path(@admin_speaker), class: 'btn btn-info btn-lg' %>
  <%= link_to t('action.back'), admin_speakers_path, class: 'btn btn-default btn-lg' %>
</div>