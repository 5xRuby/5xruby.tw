<h1><%= current_model.model_name.human %> <%= link_to t('crud.new'), new_admin_form_path, class: 'btn btn-lg btn-success' %></h1>

<%= paginate current_collection %>

<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <%= th_for Form, :id, :title, :fields %>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% current_collection.each do |form| %>
      <tr>
      <%= td_for form, :id, :title, :fields %>
        <%= td_action_for form %>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate current_collection %>
