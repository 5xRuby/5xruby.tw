<div id="activities-new">
  <h1><%= t('crud.new!', resource: Admin::Activity.model_name.human) %></h1>

  <%= render 'form' %>
  <hr>
  <%= link_to t('action.back'), admin_activities_path, class: 'btn btn-default btn-lg' %>
</div>
