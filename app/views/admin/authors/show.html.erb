<table class="table table-striped table-bordered">
  <thead>
    <th>屬性</th>
    <th>值</th>
  </thead>
  <tbody>
    <%= tr_for @admin_author, :id, :name %>
  </tbody>
</table>
<div class="btn-group">
  <%= link_to t('crud.edit'), edit_admin_author_path(@admin_author), class: 'btn btn-info btn-lg' %>
  <%= link_to t('action.back'), admin_authors_path, class: 'btn btn-default btn-lg' %>
</div>