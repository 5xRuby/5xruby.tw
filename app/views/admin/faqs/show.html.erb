<table class="table table-striped table-bordered">
  <thead>
    <th>屬性</th>
    <th>值</th>
  </thead>
  <tbody>
    <%= tr_for @admin_faq, :id, :question, :answer, :is_online %>
  </tbody>
</table>
<div class="btn-group">
  <%= link_to t('crud.edit'), edit_admin_faq_path(@admin_faq), class: 'btn btn-info btn-lg' %>
  <%= link_to t('action.back'), admin_faqs_path, class: 'btn btn-default btn-lg' %>
</div>