<table class="table table-striped table-bordered">
  <thead>
    <th>屬性</th>
    <th>值</th>
  </thead>
  <tbody>
    <%= tr_for @admin_showcase, :id, :name, :category, :description, :link, :image, :is_online, :sort_id %>
  </tbody>
</table>
<div class="btn-group">
  <%= link_to t('crud.edit'), edit_admin_showcase_path(@admin_showcase), class: 'btn btn-info btn-lg' %>
  <%= link_to t('action.back'), admin_showcases_path, class: 'btn btn-default btn-lg' %>
</div>
